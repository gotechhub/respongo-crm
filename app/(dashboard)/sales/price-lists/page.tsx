import { PageStub } from "@/components/shared/page-stub";

export default function PriceListsPage() {
  return (
    <PageStub
      title="Fiyat Listeleri"
      subtitle="Ürün bazlı fiyat listesi yönetimi."
      note="Fiyat listesi tablosu (GOLMS / GOLXP / GOCATALOG / GOFACTORY / GOTOOLS) bir sonraki adımda kuruluyor."
    />
  );
}
