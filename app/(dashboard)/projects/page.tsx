import { PageStub } from "@/components/shared/page-stub";

export default function ProjectsPage() {
  return (
    <PageStub
      title="Proje & Görev"
      subtitle="Proje → ana görev → alt görev, kanban görünümü."
      note="Kanban board (proje/görev/alt görev, çoklu atama) bir sonraki adımda kuruluyor."
    />
  );
}
